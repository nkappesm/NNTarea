<p id="notice"><%= notice %></p>

<h3>Rounds</h3>

<%= link_to 'Active Players', players_path %>
<%= link_to 'Inactive Players', players_path %>

<table>
  <thead>
    <tr>
      <th>Player</th>
      <th>Bet Amount</th>
      <th>Bet Value</th>
      <th>Result</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <!-- Need to cycle through rounds in the results table -->
    <% @results.each do |result| %>
      <tr>
        <td><%= result.bet_amount %></td>
        <td><%= link_to 'Show', round %></td>
        <td><%= link_to 'Edit', edit_round_path(round) %></td>
        <td><%= link_to 'Destroy', round, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %> 
  </tbody>
</table>

<br>

<!--<%= link_to 'New Round', new_round_path %>-->
